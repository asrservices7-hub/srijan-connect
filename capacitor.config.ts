import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.srijanbajpai.connect', appName: 'Srijan Connect', webDir: '.', bundledWebRuntime: false };
export default config;
